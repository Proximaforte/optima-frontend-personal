import { AddStyleToOtpInputDirective } from './add-style-to-otp-input.directive';

describe('AddStyleToOtpInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AddStyleToOtpInputDirective();
    expect(directive).toBeTruthy();
  });
});
