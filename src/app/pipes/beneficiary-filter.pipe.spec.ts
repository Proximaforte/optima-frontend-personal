import { BeneficiaryFilterPipe } from './beneficiary-filter.pipe';

describe('BeneficiaryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BeneficiaryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
